import type { CSSProperties } from 'react';
import type { CategoryProductEntry } from '../src/types/home';
export function livingHeroPlacement(entry: CategoryProductEntry, entries: CategoryProductEntry[]): {
  art: CSSProperties;
  pin: CSSProperties;
};
