# G:Scene — Fashion / Living 작업 정리

> Same GS, New Scene. 내가 쓰는 물건이 있는 공간에서 새로운 상품을 발견하는 쇼핑.

- 작성일: 2026-09-21
- 작업 브랜치: `feat/fashion-living-scenes`
- 기존 메인 기준 커밋: `341bc5d`
- 기능 구현 기준 커밋: `a3bd1d7`
- 구현 화면: 패션 `/fashion`, 리빙 `/living`

이 문서는 기존 미니룸 프로젝트를 바탕으로 이번 브랜치에서 진행한 **패션·리빙 화면 확장 작업**을 정리한다. 기존 메인룸, 캐릭터 이동, 옷 고르기·입어보기 등은 원래 프로젝트의 구현을 재사용했다. 전체 제품 방향은 [최종 기획](docs/ideas/doc03_final_ideation.md), 세부 설계와 검증 기록은 [카테고리 화면 설계](docs/design/category_screen_spec.md), [화면 리뷰](docs/design/category_screen_review.md)를 참고한다.

## 1. 해결하려는 고객 문제

2030 고객이 GS SHOP을 방문했을 때 상품은 많지만 “나를 위한 곳”이라고 느끼기 어렵다는 문제에서 출발했다.

| 고객 문제 | 이번 화면에서의 해결 방식 |
| --- | --- |
| 정보가 많아 무엇부터 봐야 할지 모르겠다. | 상단의 작은 내 공간과 바로 아래 추천 상품에 집중한다. 조건 선택은 한 줄로 접어둔다. |
| 나와 관계없는 상품·행사가 많다. | 내가 구매하거나 장바구니에 담은 상품을 직접 선택하면 그 상품과 연결되는 추천 이유를 보여준다. |
| 이 상품을 내가 쓰는 모습을 상상하기 어렵다. | 옷장 착장 미리보기, 코디 보드, 거실 배치 미리보기로 사용 맥락을 보여준다. |
| 무엇을 살지 정하지 않고 들어올 때도 있다. | 첫 진입부터 조건 없이 상품을 보여주고, 상황·취향·예산은 필요할 때만 선택한다. |

구매 이력은 탐색을 돕는 선택지다. 기존 상품을 반드시 골라야 쇼핑할 수 있는 구조로 만들지 않았다. 이러한 설계가 실제 고객의 탐색 부담과 구매 판단을 개선하는지는 향후 사용자 검증이 필요하다.

## 2. 기존 프로젝트와 이번 작업의 범위

| 구분 | 내용 |
| --- | --- |
| 기존 코드 재사용 | 메인 인형의 집, Fashion/Food/Living/Beauty 공간, 캐릭터 렌더러·이동·기존 동작, 구매 데이터 API, 메인 캐릭터 저장 상태 |
| 이번에 추가 | `/fashion`, `/living`, 공통 카테고리 UI, Scene 상품 API, 조건별 추천, 추천 이유, 상세·찜·장바구니, 카테고리별 공간 미리보기 |
| 메인과 연결 | Fashion/Living 라벨과 옷장·소파의 ‘추천 보기’에서 카테고리 화면으로 이동. 메인으로 돌아가는 경로 제공 |
| 이번 범위 밖 | Food/Beauty 상세 화면, 실제 회원·주문·결제, 실제 GS SHOP 데이터 연동, 학습 기반 추천 모델, AI Agent |

기존 가구 클릭과 캐릭터 상호작용은 유지하고, 구매 데이터는 기존 `/api/demo/home`을 함께 사용한다.

## 3. 최종 화면 구성과 사용자 흐름

### 기본 흐름

1. 메인 미니룸에서 Fashion 또는 Living을 선택한다.
2. 상단에서 내 옷장·거실과 구매 상품을 보고, 바로 아래에서 추천 상품을 확인한다.
3. 필요하면 구매 상품이나 장바구니 상품을 선택해 조합의 기준으로 삼는다.
4. 더 좁혀 보고 싶을 때 ‘조건 추가’를 열어 상황·취향·예산 등을 선택한다.
5. 추천 이유를 읽고 코디·공간을 미리 보거나, 상품 상세·찜·장바구니로 이동한다.

### 한눈에 보이는 화면

- 상단은 크림색·우드·차분한 녹색의 픽셀 공간으로 구성했다. 라임색은 사용하지 않았다.
- 공간의 번호 표시와 가로 상품 슬롯을 연결해 선택한 구매 상품을 알아볼 수 있게 했다.
- 구매 상품과 장바구니 목록을 전환할 수 있으며, ‘전체 보기’로 목록을 확장한다.
- 공간 아래에 추천 상품을 배치한다. 모바일은 2열, 데스크톱은 3열이다.
- 조건 요약은 한 줄 버튼으로 표시하고, 상세 옵션은 별도 대화상자에서 선택한다.
- 코디 보드와 배치 설명은 해당 미리보기를 요청했을 때만 나타난다.

방 배경의 모든 소품이 구매 상품인 것은 아니다. 번호 표시와 구매 목록에 연결된 항목으로 보유 상품을 구분한다.

### 조건 없이 시작하는 추천

최초 진입 상태는 상황 없음, 취향 없음, 예산 제한 없음, 상품 종류 전체다. 구매·장바구니 상품도 자동으로 선택하지 않는다. ‘조건 없이 추천받는 중’ 상태에서 패션 8개, 리빙 6개 예시 상품을 바로 볼 수 있다.

- 상황: 선택하지 않거나 하나를 선택한다.
- 취향: 여러 개를 선택할 수 있다.
- 예산: 제한 없음 / 3만원 / 5만원 / 10만원 이하 또는 직접 입력. **새 상품 1개 기준**이다.
- 종류·정렬: 상품 종류와 추천순·낮은 가격순을 선택한다.
- 직접 입력 예산: 1,000~10,000,000원의 정수만 허용한다.
- 변경은 적용 버튼을 눌러야 반영된다. 취소·닫기·Escape는 기존 적용 상태를 유지한다.
- 조건 초기화 후 적용하면 제한 없는 필터로 돌아간다. 별도로 선택한 조합 기준 상품은 유지된다.
- ‘새롭게 둘러보기’ 또는 구매/장바구니 목록 전환으로 조합 기준 상품을 해제한다.

## 4. 공간 인터랙션

### 패션 — 내 옷장

- 구매한 니트·셔츠를 선택하면 캐릭터가 옷으로 이동해 갈아입는 동작을 하고 돌아온다.
- 기존 메인에서 저장한 캐릭터·착장을 처음 상태로 사용한다.
- ‘착장 되돌리기’로 메인에 저장된 착장으로 돌아간다. 미리보기는 메인의 저장 상태를 바꾸지 않는다.
- 추천 상품의 ‘코디에 더하기’를 누르면 코디 보드가 나타난다.
- 같은 종류의 상품은 하나씩 조합하며, 다른 하의를 고르면 이전 하의를 교체한다.
- ‘코디 비우기’로 보드를 닫을 수 있다.

구매 니트·셔츠의 캐릭터 착장과 추천 상품의 코디 보드는 구분된다. 모든 추천 상품을 캐릭터에게 실제로 입히는 가상 피팅 기능은 아니다.

### 리빙 — 내 거실

- ‘조명 켜기/끄기’로 공간 분위기를 바꾼다.
- ‘소파 앉기/일어나기’로 기존 캐릭터 동작을 재사용한다.
- ‘내 공간에 놓아보기’로 지원 상품을 한 번에 하나씩 배치한다.
- 지원 대상은 블루 쿠션, 크림 체크 러그, 플리츠 테이블 램프, 머쉬룸 램프다.
- 다른 상품을 고르면 이전 미리보기를 교체하고, ‘되돌리기’로 제거한다.
- 러그는 테이블·소파 등 기존 가구 아래에 보이도록 겹침을 처리했다.
- 배치 시 캐릭터를 일으켜 상품이 가려지는 문제를 줄였다.

리빙 배치는 색감·분위기를 확인하는 픽셀 예시다. 실제 크기 측정이나 AR 배치를 제공하지 않는다. 전용 배치 그림이 없는 상품은 상세·장바구니 기능만 제공한다.

### 공통 동작 원칙

미리보기는 구매 이력이나 장바구니를 자동 변경하지 않는다. 착장·코디·조명·앉기·배치는 화면 안의 임시 상태이며, 찜과 장바구니만 카테고리별로 브라우저에 저장한다. 메인의 데모 초기화는 카테고리의 찜·장바구니도 초기화한다.

빠르게 연속 선택하면 마지막 선택이 적용되도록 이전 애니메이션을 취소한다. 동작 줄이기 설정을 지원하며, 대화상자·삭제·되돌리기 이후 키보드 포커스도 복구한다.

## 5. 추천 방식과 데이터 구조

현재 추천은 설명 가능한 **규칙 기반 데모**다. 실제 AI Agent나 학습된 추천 시스템은 연결하지 않았다.

| 기준 | 처리 |
| --- | --- |
| 선택한 예산·상품 종류 | 조건을 충족하는 상품만 남긴다. 예산을 넘는 상품으로 빈 결과를 채우지 않는다. |
| 기준 상품과 사전에 정의된 조합 | 추천 점수 +12, 준비된 조합 이유 표시 |
| 기준 상품과 다른 종류이면서 공통 취향 존재 | 추천 점수 +6, 공통 취향 설명 |
| 선택한 상황 일치 | 추천 점수 +4 |
| 선택한 취향 일치 | 일치 취향마다 +2 |
| 추천순 동점 | 가격 오름차순, 상품 ID 순으로 정렬 |
| 조건·기준 상품 없음 | 카테고리 전체를 표시. 현재 데모에서는 동점 정렬에 따라 가격순으로 보이며, 상품 설명을 이유로 사용 |

상황·취향은 순위에 반영하는 선호 조건이다. 예산·종류처럼 반드시 일치해야 하는 제외 조건은 아니다. 기준 상품 자체는 추천 목록에서 제외한다.

### API

| API | 역할 |
| --- | --- |
| `GET /api/demo/home` | 기존 가상 고객과 구매 상품 9개. 패션 2 / 식품 3 / 리빙 2 / 뷰티 2 |
| `GET /api/demo/scenes?category=fashion` | 패션 예시 상품 8개와 초기 장바구니 ID |
| `GET /api/demo/scenes?category=living` | 리빙 예시 상품 6개와 초기 장바구니 ID |

Scene API는 `category`, `cartIds`, `products`를 반환한다. 지원하지 않거나 누락된 카테고리는 400으로 응답한다. 처음에는 카테고리별 장바구니 예시 상품이 하나씩 있으며, 이후 사용자의 저장 상태를 복원한다.

상품에는 `id`, `category`, `name`, `brand`, `price`, `imageUrl`, `kind`, `situations`, `tastes`, `description`, `pairsWith`, `reasons`가 있다. 현재 고객·구매 내역·상품명·가격·이미지는 데모용이며 실제 판매 정보로 검증된 데이터가 아니다.

### 실데이터 연동 준비 사항 — 아직 미구현

공유받은 실제 데이터의 예상 컬럼은 다음과 같다. 아래는 연동 시 적용할 매핑 방향이며, 가져오기·변환 코드는 아직 추가하지 않았다.

| 제공 예정 컬럼 | 사용 방향 |
| --- | --- |
| `prd_id` | 문자열 상품 ID |
| `view_name` | 상품명 |
| `cate1_nm` ~ `cate4_nm` | 카테고리 계층 및 상품 종류 분류 |
| `brd_nm` | 브랜드 |
| `price` | 쉼표를 제거한 숫자 가격 |
| `domain` | fashion / living / food / beauty 공간 분류 |

추가로 필요한 것은 상품 이미지 URL, 고객별 구매·장바구니와 `prd_id`의 연결, 상황·취향 태그 및 조합 이유다. 제공 예시에는 도서가 `fashion`으로 분류된 항목이 있으므로 도메인과 상품 분류의 불일치를 검증해야 한다. 상품 카탈로그만으로 고객의 보유 여부를 판단할 수는 없다.

공간 배치에는 상품 데이터 외에도 별도의 그림·좌표 연결이 필요하다. 실상품을 가져오는 것만으로 모든 상품이 옷장이나 거실에 자동 배치되지는 않는다.

## 6. 주요 파일

| 파일 | 역할 |
| --- | --- |
| [app/fashion/page.tsx](app/fashion/page.tsx), [app/living/page.tsx](app/living/page.tsx) | 카테고리 진입 페이지 |
| [ScenePage.tsx](src/components/scene/ScenePage.tsx) | 공간·구매 목록·조건·추천·상세·찜·장바구니 공통 UI |
| [scene.css](src/components/scene/scene.css) | 픽셀 프레임, 반응형 레이아웃, 대화상자 스타일 |
| [RoomAvatar.tsx](src/components/scene/RoomAvatar.tsx), [avatar-motion.ts](src/lib/scene/avatar-motion.ts) | 기존 캐릭터 연결과 취소 가능한 이동·착장·앉기 상태 |
| [RoomPlacement.tsx](src/components/scene/RoomPlacement.tsx), [room-placement.css](src/components/scene/room-placement.css) | 리빙 배치 그림 및 가구 겹침 처리 |
| [recommend.ts](src/lib/scene/recommend.ts) | 추천 조건, 점수, 정렬, 이유 생성 |
| [scene.ts](src/types/scene.ts), [demo-scenes.ts](src/data/demo-scenes.ts) | Scene 데이터 타입과 예시 카탈로그 |
| [Scene API](app/api/demo/scenes/route.ts) | 카테고리별 예시 상품 응답 |
| [app/app.js](app/app.js), [app/index.html](app/index.html), [app/style.css](app/style.css) | 기존 메인과 추천 화면 연결 |
| [scene-art 안내](public/scene-art/README.md) | 옷장·거실·상품 데모 이미지 구성 |

## 7. 검증 기록

아래는 기능 구현 과정에서 수행한 검증이다. 이번 `main.md` 추가는 문서 변경이며 앱 전체 테스트를 새로 실행한 기록은 아니다.

- 인터랙션 구현 단계: 전체 테스트, TypeScript 검사, 프로덕션 빌드, 실행 서버 스모크 검사 통과.
- 조건 없는 기본 추천 변경 단계: 추천 테스트 9개, 프로덕션 빌드·TypeScript 검사, 브라우저 시나리오 확인 통과.
- 신규 테스트: [Scene API](tests/scenes-api.test.mjs), [추천 규칙](tests/scene-recommend.test.mjs), [캐릭터 상태 전환](tests/avatar-motion.test.mjs). 기존 메인 동작과 구매 API 테스트도 유지.
- 모바일 390×844, 작은 화면 320×740, 데스크톱 1280px에서 확인. 초기 상태에서 공간과 첫 추천 행의 이미지·이름·가격이 보이고 가로 넘침이 없도록 조정.
- 조건 적용·취소·초기화, 복수 취향, 직접 입력 예산, 잘못된 예산, 빈 결과 확인.
- 찜·장바구니 새로고침 복원, 기준 상품 해제, 상세 화면과 키보드 포커스 확인.
- 옷 갈아입기·빠른 연속 선택·되돌리기, 코디 교체, 조명·앉기·배치·취소 확인.
- 최종 기본 상태: 패션 8개 / 리빙 6개. 패션에 출근·3만원 조건 적용 시 19,000원과 29,000원 상품 2개, 초기화 적용 시 8개 복원.

상세한 발견 문제와 수정 후 재확인은 [카테고리 화면 리뷰](docs/design/category_screen_review.md)에 기록했다.

## 8. 로컬 실행과 시연

Node.js 22.18 이상이 필요하다. 저장소 루트에서 실행한다.

```bash
npm ci
npm run build
npm start
```

브라우저에서 `http://127.0.0.1:3000`을 연다. 개발 모드는 `npm run dev`를 사용할 수 있다. 이 작업 환경에서는 개발 서버의 파일 감시 한도 오류(EMFILE)가 있어 프로덕션 빌드와 실행 서버로 화면을 검증했다.

검증 명령은 다음과 같다. 스모크 검사는 실행 중인 로컬 서버가 필요하다.

```bash
npm test
npm run typecheck
npm run build
node tests/release-smoke.mjs
```

시연 순서:

1. 메인 → Fashion: 조건 없이 추천이 나오는지 확인한다.
2. 구매한 니트 선택 → 캐릭터 착장과 추천 이유 변화를 확인한다.
3. 추천 상품을 코디에 추가하고, 조건에서 상황·취향·예산을 적용한다.
4. 메인 → Living: 조명을 켜고 소파에 앉아본다.
5. 러그나 쿠션을 공간에 놓고 되돌린 뒤, 상품을 명시적으로 장바구니에 담는다.

## 9. 작업 이력과 다음 단계

| 커밋 | 작업 |
| --- | --- |
| `04956ac` | Fashion/Living 화면, Scene API, 추천·상세·찜·장바구니 추가 |
| `8f5b94d` | 픽셀 공간, 공간 아래 추천 배치, 접힌 조건 선택 UI, 모바일 개선 |
| `1220d2d` | 되돌릴 수 있는 옷장·거실 인터랙션, 코디·배치 미리보기 |
| `a3bd1d7` | 조건·자동 기준 상품 없이 추천을 시작하도록 변경 |

기능 커밋은 `feat/fashion-living-scenes`에 푸시되어 있다. 기존 Vercel 설정은 유지했으며, 이번 기능 작업의 검증은 로컬 프로덕션 서버에서 진행했다. 이 문서 작성은 배포 작업을 포함하지 않는다.

다음 우선 작업은 **실상품 데이터 변환과 고객별 구매·장바구니 연결**이다. 이후 실제 추천 결과·AI 생성 이유를 현재 API와 UI에 연결하고, 사용자가 원하는 상품을 더 쉽게 발견하는지 과업 완료 시간·추천 이유 이해도·상품 선택 행동으로 검증할 수 있다.
